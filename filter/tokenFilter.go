package filter

import (
	"github.com/gin-gonic/gin"
)

func TokenFilter(c *gin.Context) {

	//token := c.Param("token")
	//
	//if len(token) == 0 {
	//	c.String(http.StatusNotFound, "token 不能为空")
	//	c.Abort()
	//	return
	//}

	//c.Next()

}
